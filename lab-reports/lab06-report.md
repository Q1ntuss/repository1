[lab06-report.md](https://github.com/user-attachments/files/24256178/lab06-report.md)
 # Лабораторна робота 6

    **Студент:** Остапчук Богдан 
    **Група:**ЫПЗ-21

    ## 📋 Опис проєкту

   Цей вебзастосунок на Flask дозволяє користувачу переглядати список ресурсів (товарів), 
   що зберігаються на сервері через API, додавати нові записи через веб-форму та бачити повідомлення про успіх або помилки


    ## 📁 Структура проєкту

    ```
    project/
    ├── index.html       # Головна сторінка
    ├── style.css        # Стилі
    ├── script.js        # JavaScript логіка
    ├── README.md        # Цей файл
    └── backend/         # Flask API (з лаб. роботи 5)
        ├── app.py
        └── ...
    ```

    ## 🔌 API Endpoints

    Опишіть які ендпоінти використовуються:
    ##Опишіть які ендпоінти використовуються: 
    GET /items — отримує список усіх об'єктів.
    GET /items/{id} — отримує детальні дані про один об'єкт за ID.
    POST /items — створює новий об'єкт у системі.
    PUT /items/{id} — повністю замінює існуючий об'єкт новими даними.
    PATCH /items/{id} — частково оновлює лише окремі поля об'єкта.
    DELETE /items/{id} — видаляє об'єкт із бази даних.
    POST /login — виконує вхід користувача та видає токен доступу.
    GET /me — повертає профіль поточного авторизованого користувача.
    GET /health — перевіряє, чи працює сервер у штатному режимі.

    ## 📸 Скріншоти

    ### Головна сторінка
    ![Скріншот головної сторінки]()
    ![Скріншот головної сторінки] <img width="1920" height="917" alt="image" src="https://github.com/user-attachments/assets/09a3c0c9-b58e-4e4d-a7f4-37c145770226" />

    ### Додавання запису
    ![Скріншот форми](<img width="1919" height="916" alt="image" src="https://github.com/user-attachments/assets/80154e89-06e7-4dbe-8b86-88ec409433b2" />)

    ### Повідомлення про успіх
    ![Скріншот повідомлення](<img width="1920" height="922" alt="image" src="https://github.com/user-attachments/assets/e39a099d-045a-4b2d-985f-64522e600ad6" />)

    ## 🔗 Посилання
    - [Посилання на GitHub] https://github.com/Q1ntuss/repository1/tree/feature/user-authetication-ostapchuk/lab-reports

    ## ✅ Висновки
    Я навчився створювати api-demo, для того щоб покращити вебзастосунок для подальшого додавання ресурсів(товарів) а також створювати дезайн для сторінок сайту.
